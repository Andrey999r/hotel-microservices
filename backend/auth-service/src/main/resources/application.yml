server:
  port: 8081
spring:
  application:
    name: auth-service
  config:
    import: 'optional:configserver:'

management:
  endpoints:
    web:
      exposure:
        include: "*"
  prometheus:
    metrics:
      export:
        enabled: true
